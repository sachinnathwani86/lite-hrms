"""The product is configured through the first-run /setup screen.

Run `flask db upgrade`, start the server, and visit `/setup` to create the
company profile and first administrator. No demo employees or credentials are
seeded by this script.
"""

print("Lite HRMS uses the /setup screen for first-run configuration; no demo data was created.")
